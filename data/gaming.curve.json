{
    "activation_temp": 45,
    "max_temp": 80,
    "min_rpm": 1500,
    "max_rpm": 7000,
    "equation": "(-1 if current_temp < activation_temp else (max_rpm if current_temp > max_temp else (min_rpm + (max_rpm - min_rpm) * ((current_temp - activation_temp)**2 / (max_temp - activation_temp)**2))))"
  }
  